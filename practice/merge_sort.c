#include <#cs50.h>
#include <stdio.h>
#include <stdlib.h>

int main (int argc, string argv[])
{
    // Convert string array into integer array
    int numbers[argc - 1];
    for (n = 1; n < argc; n++)
    {
        numbers[n - 1] = argv[n];
    }

    // Sort list using merge sort
    
}
